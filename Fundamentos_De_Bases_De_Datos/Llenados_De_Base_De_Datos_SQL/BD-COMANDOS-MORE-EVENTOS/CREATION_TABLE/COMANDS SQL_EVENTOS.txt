creacion de tabla

CREATE TABLE EVENTOS
(CODIGO INT(5) PRIMARY KEY,CODIGO_ANIMADOR INT(5),ID_PRESENTADOR INT(5),ID_CLIENTE INT(5),DESCRIPCION TEXT,VALOR_BASE INT(5), HORARIO TIME, FECHA DATE);

CREATE TABLE ANIMADOR
(CODIGO_ANIMADOR INT(5) PRIMARY KEY,MODELO_DISFRAZ VARCHAR(20),APELLIDO TEXT);

CRATE TABLE PRESENTADOR 
(ID_PRESENTADOR INT(5) PRIMARY KEY, NOMBRE TEXT);

CREATE TABLE DISFRAZ
(MODELO_DISFRAZ VARCHAR(20) PRIMARY KEY,NOMBRE TEXT,TALLA DOUBLE(4));

CREATE TABLE CLIENTE
(ID_CLIENTE INT(5) PRIMARY KEY,NOMBRE TEXT,APELLIDO TEXT,DOMICILIO TEXT,TELEFONO BIGINT(13));



llaves

EVENTO

CREATE INDEX animador ON EVENTO (CODIGO_ANIMADOR)

CREATE INDEX animador ON EVENTO (ID_PRESENTADOR)

CREATE INDEX animador ON EVENTO (ID_CLIENTE)


ANIMADOR

CREATE INDEX animador ON EVENTO (MODELO_DISFRAZ)



relaciones

ALTER TABLE eventos ADD FOREIGN KEY (ID_PRESENTADOR) REFERENCES presentador (ID_PRESENTADOR) ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE eventos ADD FOREIGN KEY (CODIGO_ANIMADOR) REFERENCES animador (CODIGO_ANIMADOR) ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE eventos ADD FOREIGN KEY (ID_CLIENTE) REFERENCES cliente (ID_CLIENTE) ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE animador ADD FOREIGN KEY (MODELO_DISFRAZ) REFERENCES disfraz (MODELO_DISFRAZ) ON DELETE CASCADE ON UPDATE CASCADE;

